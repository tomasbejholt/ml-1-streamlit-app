name: Keep Render alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping churn-backend
        run: curl https://churn-backend-wz05.onrender.com
